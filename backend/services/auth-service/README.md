# Auth Service

Node.js + Express + Sequelize + Neon PostgreSQL authentication service.

## Features
- Register/Login
- JWT Authentication
- PostgreSQL (Neon)
- Sequelize ORM

## Run
npm install
npm run dev